{
  "name": "@topgunbuild/example-collaborative-tasks",
  "version": "0.10.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build:client": "esbuild src/public/app.js --bundle --outfile=src/public/app.bundle.js --format=iife --platform=browser",
    "dev": "pnpm run build:client && tsx src/server.ts",
    "test": "echo \"No tests for example app\" && exit 0"
  },
  "dependencies": {
    "@topgunbuild/client": "workspace:*",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/pg": "^8.10.9",
    "esbuild": "^0.19.0",
    "tsx": "^4.7.0",
    "typescript": "^5.0.0"
  }
}
