{
  "name": "push-worker",
  "version": "0.10.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "generate-vapid": "node scripts/generate-vapid.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "wrangler": "^4.94.0"
  }
}
