================================================================================
SPEC-360 — EXECUTED WITNESS ARMS
================================================================================

One section per witness. Each section records: the mutation applied verbatim,
the exact output it produced, the revert, and the re-run that returned green.
A green test without its arm is an UNEXERCISED negative, not an EARNED one.

Sections are appended by the group that owns the witness. Do NOT reformat or
re-order sections already present; append your own beneath the last one.

Section index (filled in as each group lands its witness):
  W7  — enumeration completeness, mechanical                    [below]
  W1  — unmutated baseline                                      [below]
  W3  — planted positive control for the classifier             [below]
  W2  — RECIPE ONLY, execution deferred to G4                   [below]
  ORIGIN SIGNATURE — the planted antecedent's own removal line   [below]
  W8  — the instrument's POSITIVE control                       [below]
  W4  — the instrument's NEGATIVE control, both paths           [below]
  W5  — the instrument's OBSERVATION arm                        [below]
  W6  — ARM SELECTION only; implementation is G6's              [below]

================================================================================
W7 — ENUMERATION COMPLETENESS (MECHANICAL SOURCE TEST)
================================================================================

Owner group : G3
Branch      : sf-360-origin-instrument
Test        : tombstone_frontier_impl::tests::
              epoch_tags_has_five_production_writers_and_the_drain_has_one_caller
Landed in   : packages/server-rust/src/tombstone_frontier_impl.rs  (counted 2/5)
Commit      : fd157593
Scan surface (pinned, and the ONLY thing the claim covers):
    include_str!("tombstone_frontier_impl.rs")
    include_str!("service/domain/crdt.rs")

Named residual, carried in the test's own doc-contract: a production caller
added in a THIRD, UNSCANNED file would NOT RED. Closing that gap is deferred
and tracked in TODO-634.

--------------------------------------------------------------------------------
W7 — HOW #[cfg(test)] CODE WAS SCOPED OUT, VERIFIED EMPIRICALLY
--------------------------------------------------------------------------------

The file's test module DOES touch the map, so the scoping is load-bearing
rather than decorative. Verified at HEAD before the test was written and
re-verified at commit fd157593 after it landed (line numbers below are the
post-landing ones; none of the production sites moved):

  $ grep -n 'epoch_tags' packages/server-rust/src/tombstone_frontier_impl.rs

  production receiver is `self.` at    : 524 611 683 728 855 869 958 979 1036 1081
  test-module receiver is `state.` at  : 3099 3241 3265 3512 5266 5565
  this test's own doc-comment mentions : 4026 4043 4067 4088 4090
  doc-comment / field-decl mentions    : 163 273 303 350 663 747 845 975 (+ test module)

  $ grep -n '^#\[cfg(test)\]' packages/server-rust/src/tombstone_frontier_impl.rs
  2802
  2820
  $ grep -n '^#\[cfg(test)\]' packages/server-rust/src/service/domain/crdt.rs
  1753

SCOPING RULE ADOPTED: each scanned file is cut at the FIRST column-0
`#[cfg(test)]` attribute (`production_prefix`), and only the prefix is scanned.
For tombstone_frontier_impl.rs that cut is line 2802 — above every `state.`
access and above every test-module drain call. For crdt.rs it is line 1753 —
above the four test-module drain calls at :5387, :5846, :5893, :6149.

Two consequences relied on:
  (a) The scan does not have to distinguish `self.` from `state.` receivers by
      hand; the cut already removed every test access. Receiver-token filtering
      alone would have been fragile, because a future test could write `self.`
      inside an `impl` block in the test module.
  (b) The scanning literals may be spelled out. They live in the test module,
      BELOW the cut, so a scan that spells the thing it counts cannot count
      itself — the hazard this file's neighbouring structural test
      guards at :4252 by composing its needle instead.

The non-mutating reads are NOT miscounted as writers. Two of them are written
as broken method chains (`self` on one line, `.epoch_tags` on the next):
:727-728 `.get(&epoch)` and :956-958 `.keys()`. A naive scan for the literal
`self.epoch_tags` would have MISSED them entirely, and a scan for `epoch_tags.`
would have counted them as unclassified. Hence `flattened()`, which collapses
whitespace runs and rejoins ` .` to `.` before scanning. All five reads are then
classified by the verb that follows: `len` (:611), `contains_key` (:683),
`get` (:728), `keys` (:958), and the empty verb — the whole-map borrow at :1081,
`for (&epoch, refs) in &self.epoch_tags`.

--------------------------------------------------------------------------------
W7 — GREEN BASELINE (unmutated tree, at commit fd157593)
--------------------------------------------------------------------------------

$ SDKROOT=$(/usr/bin/xcrun --sdk macosx --show-sdk-path) \
  cargo test --release -p topgun-server --lib tombstone_frontier

    Finished `release` profile [optimized] target(s)
     Running unittests src/lib.rs (target/release/deps/topgun_server-b3ef059854b88584)

running 71 tests
...
test tombstone_frontier_impl::tests::epoch_tags_has_five_production_writers_and_the_drain_has_one_caller ... ok
...
test result: ok. 71 passed; 0 failed; 0 ignored; 0 measured; 1788 filtered out; finished in 10.74s

--------------------------------------------------------------------------------
W7 — ARM A1: A SIXTH WRITER WITH AN ALREADY-ENUMERATED VERB
--------------------------------------------------------------------------------

MUTATION APPLIED — packages/server-rust/src/tombstone_frontier_impl.rs, inserted
after :855 (`self.epoch_tags.clear();`), inside the rebuild path:

    $ sed -i '' '855a\
            self.epoch_tags.remove(&0);
    ' packages/server-rust/src/tombstone_frontier_impl.rs

    854         self.rebuild_cleared_refs_total += self.indexed_refs;
    855         self.epoch_tags.clear();
    856 +       self.epoch_tags.remove(&0);
    857         self.epoch_max_seq.clear();

OUTPUT — RED:

$ SDKROOT=$(/usr/bin/xcrun --sdk macosx --show-sdk-path) \
  cargo test --release -p topgun-server --lib epoch_tags_has_five

running 1 test
test tombstone_frontier_impl::tests::epoch_tags_has_five_production_writers_and_the_drain_has_one_caller ... FAILED

---- tombstone_frontier_impl::tests::epoch_tags_has_five_production_writers_and_the_drain_has_one_caller stdout ----

thread '...' panicked at packages/server-rust/src/tombstone_frontier_impl.rs:4153:9:
assertion `left == right` failed: `self.epoch_tags`'s production access shapes moved. The enumeration this file's origin reasoning rests on is: Stamp and Restore push (`entry`), RebuildClear clears, RebuildInsert inserts, DrainRemove removes — five mutations — beside five non-mutating reads. Re-verify the writer record before re-baselining this map.
  left: {"": 1, "clear": 1, "contains_key": 1, "entry": 2, "get": 1, "insert": 1, "keys": 1, "len": 1, "remove": 2}
 right: {"": 1, "clear": 1, "contains_key": 1, "entry": 2, "get": 1, "insert": 1, "keys": 1, "len": 1, "remove": 1}

test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 1858 filtered out; finished in 0.00s
error: test failed, to rerun pass `-p topgun-server --lib`

The failure NAMES the moved shape: `remove` went 1 -> 2. The per-verb tally, not
a bare total, is what makes the message diagnostic.

REVERT:

    $ git checkout -- packages/server-rust/src/tombstone_frontier_impl.rs

--------------------------------------------------------------------------------
W7 — ARM A2: A SIXTH WRITER WITH AN UNENUMERATED VERB (THE FAIL-CLOSED ARM)
--------------------------------------------------------------------------------

Arm A1 only proves the test notices a writer whose SHAPE it already knows. The
harder case — and the one a real sixth writer is likelier to take — is a shape
nobody enumerated. A scan that silently skips what it does not recognise would
report green on exactly the mutation it exists to catch.

MUTATION APPLIED — same insertion point, a `retain` this time. It is also the
only enumerated-or-not shape that can leave an entry PRESENT-AND-EMPTY's dual
(it removes empties), which makes it a fair stand-in for an unenumerated writer
with real semantics:

    $ sed -i '' '855a\
            self.epoch_tags.retain(|_, v| !v.is_empty());
    ' packages/server-rust/src/tombstone_frontier_impl.rs

    855         self.epoch_tags.clear();
    856 +       self.epoch_tags.retain(|_, v| !v.is_empty());
    857         self.epoch_max_seq.clear();

OUTPUT — RED:

$ SDKROOT=$(/usr/bin/xcrun --sdk macosx --show-sdk-path) \
  cargo test --release -p topgun-server --lib epoch_tags_has_five

running 1 test
test tombstone_frontier_impl::tests::epoch_tags_has_five_production_writers_and_the_drain_has_one_caller ... FAILED

---- tombstone_frontier_impl::tests::epoch_tags_has_five_production_writers_and_the_drain_has_one_caller stdout ----

thread '...' panicked at packages/server-rust/src/tombstone_frontier_impl.rs:4126:26:
unclassified `self.epoch_tags` access `.retain(` — this scan only knows the five enumerated mutations and the five enumerated reads, so a new access shape must be classified here before it can be counted. Context was:
.retain(|_, v| !v.is_empty()); self.epoch_max_seq.clear(); let width = self.epoch_width.max(1); self.current_epoch = e_rec; self.current_max_epoch = e_rec; // P

test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 1858 filtered out; finished in 0.00s
error: test failed, to rerun pass `-p topgun-server --lib`

The classifier is CLOSED, not open: an access shape outside the table panics by
name and prints its context rather than falling through as "not a writer".

REVERT:

    $ git checkout -- packages/server-rust/src/tombstone_frontier_impl.rs

--------------------------------------------------------------------------------
W7 — ARM B: A SECOND PRODUCTION CALLER OF THE DRAIN
--------------------------------------------------------------------------------

MUTATION APPLIED — packages/server-rust/src/service/domain/crdt.rs, inserted
after :1521, i.e. a second production route into the drain inside
`prune_epoch_tombstones` itself:

    $ sed -i '' '1521a\
        let _second_route = frontier.drain_prunable_tombstones();
    ' packages/server-rust/src/service/domain/crdt.rs

    1521    let drained = frontier.drain_prunable_tombstones();
    1522 +  let _second_route = frontier.drain_prunable_tombstones();
    1523    // One record per invocation, empty drains included: ...

Note this mutation is in the OTHER scanned file and touches no line of
tombstone_frontier_impl.rs. The test still recompiled and still RED'd, because
`include_str!` makes the scanned file a build dependency of the scanning test —
so the guard cannot go stale against an unchanged scanner.

OUTPUT — RED:

$ SDKROOT=$(/usr/bin/xcrun --sdk macosx --show-sdk-path) \
  cargo test --release -p topgun-server --lib epoch_tags_has_five

running 1 test
test tombstone_frontier_impl::tests::epoch_tags_has_five_production_writers_and_the_drain_has_one_caller ... FAILED

---- tombstone_frontier_impl::tests::epoch_tags_has_five_production_writers_and_the_drain_has_one_caller stdout ----

thread '...' panicked at packages/server-rust/src/tombstone_frontier_impl.rs:4216:9:
assertion `left == right` failed: `drain_prunable_tombstones` must have exactly ONE production caller across the two scanned files. A second production route into the drain would mean an exit row could be emitted by a drain that no pass record wraps, which is the premise the divergence reasoning rests on. Found:
[
    (
        "service/domain/crdt.rs",
        " &KeyWriterRegistry, ) { let drained = frontier.drain_prunable_tombstones",
    ),
    (
        "service/domain/crdt.rs",
        "able_tombstones(); let _second_route = frontier.drain_prunable_tombstones",
    ),
]
  left: 2
 right: 1

test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 1858 filtered out; finished in 0.00s
error: test failed, to rerun pass `-p topgun-server --lib`

The failure prints each call site with its lead-in text, so the second route is
identifiable from the message alone.

REVERT:

    $ git checkout -- packages/server-rust/src/service/domain/crdt.rs
    $ git status --porcelain
    (clean)

--------------------------------------------------------------------------------
W7 — GREEN RE-RUN AFTER ALL THREE REVERTS
--------------------------------------------------------------------------------

$ SDKROOT=$(/usr/bin/xcrun --sdk macosx --show-sdk-path) \
  cargo test --release -p topgun-server --lib tombstone_frontier

running 71 tests
...
test tombstone_frontier_impl::tests::epoch_tags_has_five_production_writers_and_the_drain_has_one_caller ... ok
test tombstone_frontier_impl::tests::tier1_frontierstate_arm_drives_every_reachable_class_of_the_frozen_walk ... ok
test tombstone_frontier_impl::persistence_tests::concurrent_confirm_then_forget_never_resurrects_durable_cursor ... ok

test result: ok. 71 passed; 0 failed; 0 ignored; 0 measured; 1788 filtered out; finished in 10.80s

--------------------------------------------------------------------------------
W7 — VERDICT
--------------------------------------------------------------------------------

EARNED. The guard REDs on a sixth writer whose verb it already knows (A1), on a
sixth writer whose verb it does not (A2, fail-closed), and on a second
production caller in the other scanned file (B). It returns green on the
unmutated tree. The claim it earns is scoped to the two pinned files and no
wider; the third-unscanned-file residual is named in the test's doc-contract
and routed to TODO-634.


================================================================================
W1 — UNMUTATED BASELINE
================================================================================

Owner group : G2
Branch      : sf-360-origin-instrument
Tree state  : commit e99635de (G1 + G3 landed; no mutation applied, no W3 yet)

W1 is the only witness in this file that carries NO mutation. Its whole content
is the assertion the other witnesses are read against: the three suites are
green BEFORE anything is planted, mutated or instrumented. A mutation arm that
REDs against a tree that was already red proves nothing about the mutation.

--------------------------------------------------------------------------------
W1 — LEG 1: the tombstone_frontier suite
--------------------------------------------------------------------------------

$ SDKROOT=$(/usr/bin/xcrun --sdk macosx --show-sdk-path) \
  cargo test --release -p topgun-server --lib tombstone_frontier

    Finished `release` profile [optimized] target(s)
     Running unittests src/lib.rs (target/release/deps/topgun_server-b3ef059854b88584)

running 71 tests
...
test tombstone_frontier_impl::tests::step0_leg_c_v1_positive_control_planted_ref_loss_fires_on_the_returned_record ... ok
test tombstone_frontier_impl::tests::epoch_tags_has_five_production_writers_and_the_drain_has_one_caller ... ok
test tombstone_frontier_impl::tests::tier1_frontierstate_arm_drives_every_reachable_class_of_the_frozen_walk ... ok
test tombstone_frontier_impl::persistence_tests::concurrent_confirm_then_forget_never_resurrects_durable_cursor ... ok

test result: ok. 71 passed; 0 failed; 0 ignored; 0 measured; 1788 filtered out; finished in 10.60s

--------------------------------------------------------------------------------
W1 — LEG 2: the crdt suite
--------------------------------------------------------------------------------

$ SDKROOT=$(/usr/bin/xcrun --sdk macosx --show-sdk-path) \
  cargo test --release -p topgun-server --lib crdt

running 90 tests
...
test service::domain::crdt::tests::prune_exit_ledger_sums_to_considered ... ok
test service::domain::crdt::tests::prune_record_armed_disarmed_gauge_neutral ... ok
test service::domain::crdt::tests::the_witness_methods_reach_exactly_one_production_implementor ... ok

test result: ok. 90 passed; 0 failed; 0 ignored; 0 measured; 1769 filtered out; finished in 0.22s

--------------------------------------------------------------------------------
W1 — LEG 3: the sim suite, AND THE RUN-COUNT RECONCILIATION
--------------------------------------------------------------------------------

$ SDKROOT=$(/usr/bin/xcrun --sdk macosx --show-sdk-path) pnpm test:sim
  (= cargo test --profile ci-sim --features simulation -p topgun-server -- sim)

The lib target reports:

  running 27 tests
  test result: ok. 27 passed; 0 failed; 0 ignored; 0 measured; 1857 filtered out

TWENTY-SEVEN, NOT TWENTY-FIVE — and that is NOT a failed check. `sim` is a
SUBSTRING filter over test paths, not a module selector, so it catches two
tests that have nothing to do with the simulation harness. Enumerated in full
so the reconciliation is checkable rather than asserted:

  src/sim/* attributes .......................................... 25
    sim::cluster::tests                                           10
      live_top_n_negative_control_membership_stays_two
      live_top_n_window_holds_under_fault
      sim_query_filter_sort_limit_under_node_failure
      dag_execute_roundtrip_resolves_completion
      single_node_cursor_roundtrip_has_more_flips_at_exhaustion
      query_under_partition_returns_alive_node_results
      query_filter_sort_limit_routes_through_dag
      distributed_merge_global_sort_and_limit_under_partition
      distributed_keyset_cursor_under_partition
      paged_ws_query_under_partition_emits_cursor_and_correct_follow_up
    sim::tombstone_gc_proof::tests                                12
      d1_divergence_removed_observation_diverges_from_entry_attribution
      d2_post_exit_restore_reappears_without_a_new_exit_row
      d3_absent_key_settlement_shows_zero_reclaim_despite_positive_attribution
      d5_sequential_regime_bridge_identities_and_p_keys_hold_over_3000_epochs
      forgotten_client_resurrects_dark_gate_blocks_armed
      lagging_known_replica_repush_suppressed_union_admitted
      merkle_roundtrip_does_not_advance_gated_delivered_push_still_rejected
      merkle_roundtrip_is_readonly_wrt_or_value_state
      reused_connection_regressed_then_stale_push_rejected
      interleaved_prune_during_push_never_resurrects
      prune_epoch_residency_discrimination_under_network_fault
      reclamation_boundary_holds_when_a_sweep_races_a_second_devices_ack
    sim::or_churn_inplace::tests                                   1
      or_churn_across_partition_converges_inplace
    sim::or_delta_recovery::tests                                  2
      or_churn_recovers_to_the_partition_converged_state
      or_recovery_is_invariant_under_the_framing_switch
  caught by the `sim` SUBSTRING, unrelated to the harness ......... 2
      dag::converter::tests::convert_simple_query_single_node
      service::domain::predicate::tests::predicate_regex_simple_match
        Both match because libtest's filter is a plain substring test over the
        FULL test path, and `sim` occurs inside `sim`ple. Neither test touches
        the simulation harness.
                                                                  --
  total                                                           27

OBSERVED NUMBER: 27. EXPECTED NUMBER: 27. Reconciled.

Two further targets in the same invocation are NOT part of the 27 and are
recorded so a future reader does not mistake a different total for drift:

  Running tests/sim_cluster.rs      : running 7 tests   — 7 passed
  Running tests/proptest_sim.rs     : running 4 tests   — 4 passed
  Doc-tests topgun_server           : src/sim/mod.rs (line 15) ... ignored
  (nine further test binaries report `running 0 tests` — everything filtered)

Whole-invocation total: 27 + 7 + 4 = 38 passed, 0 failed.

--------------------------------------------------------------------------------
W1 — VERDICT
--------------------------------------------------------------------------------

GREEN on all three legs with no mutation applied. The baseline every arm below
is read against is established.

================================================================================
W3 — PLANTED POSITIVE CONTROL FOR THE CLASSIFIER
================================================================================

Owner group : G2
Branch      : sf-360-origin-instrument
Test        : tombstone_frontier_impl::tests::
              a_planted_empty_tag_vector_is_the_only_arm_that_attributes_without_observation
Landed in   : packages/server-rust/src/tombstone_frontier_impl.rs  (counted 2/5)
Commit      : 4974b63d

WHAT THIS WITNESS CLAIMS, AND WHAT IT DOES NOT.
It claims that `classify_drain_attribution` returns `AttributedWithoutObservation`
on a record pair a REAL drain produced from a planted antecedent, and does NOT
return it on an unplanted drain over the SAME fixture. It is a classifier
control. It is NOT the service-level consequence: the drive through
`prune_epoch_tombstones`, with both records reconstructed off the rendered
transport, is a separate obligation and is deliberately not pre-empted here.
The classifier's pure-input totality tests live beside its definition in
`tombstone_frontier.rs` and are a third, different thing.

--------------------------------------------------------------------------------
W3 — HOW THE ANTECEDENT IS PLANTED
--------------------------------------------------------------------------------

Two private helpers in this file's existing `#[cfg(test)]` module, plus the
test itself. No injection constructor, no capturing-observer seam, no test-only
`pub` / `pub(crate)` surface was added to `TombstoneFrontier`; the diff is a
pure insertion into the test module (165 added lines, 0 removed).

  fn pass_record_from_drained(&[(Epoch, TombstoneRef)]) -> PrunePassRecord
      Mirrors the service's own construction off the drain's RETURN VALUE:
      `considered` = one per returned ref, `epochs_drained` = distinct epochs,
      `empty_drain` = `drained.is_empty()`. Derived, never hand-written, so the
      pass half of the pair stays a function of what the drain returned.

  fn drain_one_epoch_fixture(plant_empty_tag_vector: bool)
        -> (Vec<PruneEpochResidencyRecord>, Vec<(Epoch, TombstoneRef)>)
      width 1; stamp k1/TAG1 into epoch 1; stamp k2/TAG2 to roll epoch 1 over
      (so its slot has `entry_emitted == true` and `refs_at_entry == 1`);
      `set_durable_epoch_watermark(1)`; `set_delivered(CONN_A, 100)`;
      `confirm_apply_ack("a5:drain-attribution|dev-1", 2, CONN_A)`.
      Then, under the in-module bypass:

          let mut state = f.lock();                       // :5634
          if plant_empty_tag_vector {
              state.epoch_tags.insert(1, Vec::new());     // :5636
          }
          let (drained, _split, exits) = state.drain_prunable();
          (exits, drained)

      The plant replaces epoch 1's entry with a PRESENT-BUT-EMPTY `Vec`, so the
      drain's `epoch_tags.remove(&1)` still returns `Some` — a genuine
      `DrainedByPrune` attribution against a slot whose entry-side bytes are
      nonzero — while the removed vector carries nothing to observe.

      This is the same `f.lock()` shape the SPEC-358 Step 0 leg (c) control
      already uses at :5560 / :5566; the anchors the spec records as :5344 /
      :5350 have drifted to those lines at HEAD.

--------------------------------------------------------------------------------
W3 — GREEN BASELINE (unmutated tree, at commit 4974b63d)
--------------------------------------------------------------------------------

$ SDKROOT=$(/usr/bin/xcrun --sdk macosx --show-sdk-path) \
  cargo test --release -p topgun-server --lib a_planted_empty_tag_vector

running 1 test
test tombstone_frontier_impl::tests::a_planted_empty_tag_vector_is_the_only_arm_that_attributes_without_observation ... ok

test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 1859 filtered out; finished in 0.00s

The values the two arms actually carried (read off the arm-A and arm-C failure
output below, which prints both records in full):

  PLANTED   exit: epoch 1, refs_at_entry 1, refs_at_exit 0, stamped_bytes 4,
                  bytes_freed_attributed 4, exit_kind DrainedByPrune,
                  removed_refs_observed 0, removed_bytes_observed 0
            pass: considered 0, epochs_drained 0, empty_drain true
            class: AttributedWithoutObservation

  CONTROL   exit: epoch 1, refs_at_entry 1, refs_at_exit 0, stamped_bytes 4,
                  bytes_freed_attributed 4, exit_kind DrainedByPrune,
                  removed_refs_observed 1, removed_bytes_observed 4
            pass: considered 1, epochs_drained 1, empty_drain false
            class: ObservedAndCounted

Same epoch, same exit kind, same attributed bytes. The ONLY thing that differs
is whether anything was observed — which is exactly the discrimination the
class is supposed to make.

--------------------------------------------------------------------------------
W3 — ARM A: REMOVE THE PLANT (does the antecedent do the work?)
--------------------------------------------------------------------------------

If the fixture produced the divergent class on its own, the plant would be
decoration. This arm makes the planted branch a no-op while leaving every
assertion intact.

MUTATION APPLIED — packages/server-rust/src/tombstone_frontier_impl.rs, inside
`drain_one_epoch_fixture`:

        if plant_empty_tag_vector {
    -       state.epoch_tags.insert(1, Vec::new());
    +       let _ = 1;
        }

OUTPUT — RED:

running 1 test
test tombstone_frontier_impl::tests::a_planted_empty_tag_vector_is_the_only_arm_that_attributes_without_observation ... FAILED

thread '...' panicked at packages/server-rust/src/tombstone_frontier_impl.rs:5680:9:
assertion `left == right` failed: the planted vector carried nothing for the removal site to observe: PruneEpochResidencyRecord { epoch: 1, refs_at_entry: 1, refs_at_exit: 0, stamped_bytes: 4, bytes_freed_attributed: 4, exit_kind: DrainedByPrune, entered_at_op_seq: 1, entered_at_unix_ms: 1787501061498, exited_at_op_seq: 2, lwm_passed_at_op_seq: Some(2), fence_passed_at_op_seq: Some(2), lwm_at_exit: 2, durable_watermark_at_exit: 1, current_epoch_at_exit: 2, removed_refs_observed: 1, removed_bytes_observed: 4 }
  left: 1
 right: 0

test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 1859 filtered out; finished in 0.00s
error: test failed, to rerun pass `-p topgun-server --lib`

It REDs on the ANTECEDENT itself (`removed_refs_observed == 0`), before the
class is ever computed, and prints the whole record. Without the plant the same
fixture observes its one real ref — so the antecedent is what produces the
state, not the fixture.

REVERT:

    (the `state.epoch_tags.insert(1, Vec::new());` line restored)

--------------------------------------------------------------------------------
W3 — ARM B: HARD-WIRE THE PASS HALF TO THE EMPTY READING
--------------------------------------------------------------------------------

The pass half is DERIVED from the drain's return value on purpose. This arm
replaces the derivation with the constant the planted arm expects, which is the
shape a control that hand-wrote its pass record would have shipped.

MUTATION APPLIED — packages/server-rust/src/tombstone_frontier_impl.rs, inside
`pass_record_from_drained`:

    -   considered: u64::try_from(drained.len()).unwrap_or(u64::MAX),
    +   considered: 0,
        epochs_drained: u64::try_from(distinct.len()).unwrap_or(u64::MAX),
    -   empty_drain: drained.is_empty(),
    +   empty_drain: true,

OUTPUT — RED:

running 1 test
test tombstone_frontier_impl::tests::a_planted_empty_tag_vector_is_the_only_arm_that_attributes_without_observation ... FAILED

thread '...' panicked at packages/server-rust/src/tombstone_frontier_impl.rs:5722:9:
assertion `left == right` failed: pass: PrunePassRecord { considered: 0, dropped: 0, matched_nothing: 0, absent: 0, restored_read_error: 0, restored_evicted: 0, restored_write_error: 0, bytes_freed: 0, epochs_drained: 1, empty_drain: true }
  left: 0
 right: 1

test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 1859 filtered out; finished in 0.00s
error: test failed, to rerun pass `-p topgun-server --lib`

RECORDED HONESTLY: this arm REDs on the CONTROL's pass-shape guard
(`considered == 1`), which sits ABOVE the class comparison — so it proves the
pass half is really derived, but it does NOT reach the discriminating
`assert_ne!`. Arm C exists because of that gap.

REVERT:

    (the two derived expressions restored)

--------------------------------------------------------------------------------
W3 — ARM C: COLLAPSE THE CLASSIFIER'S OBSERVATION SIDE
--------------------------------------------------------------------------------

The one arm that reaches the discriminating assertion. A classifier that named
the divergent class unconditionally would satisfy the planted arm and every
guard; only the unplanted control can catch it.

MUTATION APPLIED — packages/server-rust/src/tombstone_frontier.rs, inside
`classify_drain_attribution` (transient; the file is byte-restored below and is
NOT modified by this group):

    -   let observed = exit.removed_refs_observed > 0 || pass.considered > 0 || !pass.empty_drain;
    +   let observed = false;

OUTPUT — RED:

running 1 test
test tombstone_frontier_impl::tests::a_planted_empty_tag_vector_is_the_only_arm_that_attributes_without_observation ... FAILED

thread '...' panicked at packages/server-rust/src/tombstone_frontier_impl.rs:5730:9:
assertion `left != right` failed: unplanted control must NOT read as the divergent class; exit PruneEpochResidencyRecord { epoch: 1, refs_at_entry: 1, refs_at_exit: 0, stamped_bytes: 4, bytes_freed_attributed: 4, exit_kind: DrainedByPrune, entered_at_op_seq: 1, entered_at_unix_ms: 1787501677865, exited_at_op_seq: 2, lwm_passed_at_op_seq: Some(2), fence_passed_at_op_seq: Some(2), lwm_at_exit: 2, durable_watermark_at_exit: 1, current_epoch_at_exit: 2, removed_refs_observed: 1, removed_bytes_observed: 4 }, pass PrunePassRecord { considered: 1, dropped: 0, matched_nothing: 0, absent: 0, restored_read_error: 0, restored_evicted: 0, restored_write_error: 0, bytes_freed: 0, epochs_drained: 1, empty_drain: false }
  left: AttributedWithoutObservation
 right: AttributedWithoutObservation

test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 1859 filtered out; finished in 0.00s
error: test failed, to rerun pass `-p topgun-server --lib`

The planted arm still PASSED under this mutation — which is the whole point.
Only the `assert_ne!` on the unplanted control caught it, and its message
prints both records so the reader can see that a pair with
`removed_refs_observed = 1`, `considered = 1`, `empty_drain = false` was
nonetheless named `AttributedWithoutObservation`.

REVERT:

    $ git checkout -- packages/server-rust/src/tombstone_frontier.rs
    $ git status --porcelain
     M packages/server-rust/src/tombstone_frontier_impl.rs     (W3 itself)

--------------------------------------------------------------------------------
W3 — GREEN RE-RUN AFTER ALL THREE REVERTS
--------------------------------------------------------------------------------

$ SDKROOT=$(/usr/bin/xcrun --sdk macosx --show-sdk-path) \
  cargo test --release -p topgun-server --lib tombstone_frontier

running 72 tests
...
test tombstone_frontier_impl::tests::a_planted_empty_tag_vector_is_the_only_arm_that_attributes_without_observation ... ok
test tombstone_frontier_impl::tests::tier1_frontierstate_arm_drives_every_reachable_class_of_the_frozen_walk ... ok
test tombstone_frontier_impl::persistence_tests::concurrent_confirm_then_forget_never_resurrects_durable_cursor ... ok

test result: ok. 72 passed; 0 failed; 0 ignored; 0 measured; 1788 filtered out; finished in 11.57s

(71 at W1's baseline, 72 with W3 added.)

--------------------------------------------------------------------------------
W3 — VERDICT
--------------------------------------------------------------------------------

EARNED. The control REDs when the antecedent is removed (A), when the pass half
stops being derived from the drain (B), and when the classifier's observation
side is collapsed so the divergent class is named unconditionally (C) — and C
is caught by the unplanted arm alone, which is what makes the second arm
load-bearing rather than decorative. It returns green on the unmutated tree.

================================================================================
W2 — RECIPE (EXECUTION DEFERRED TO G4)
================================================================================

Owner group : recipe by G2; EXECUTED TRANSCRIPT OWED BY G4
Branch      : sf-360-origin-instrument
Status      : NOT EXECUTED. This section is a recipe, not a witness.

WHY THIS SECTION IS A RECIPE AND NOT A TRANSCRIPT.
W2's arm grades the CONSEQUENCE DRIVE — the planted antecedent driven through
`prune_epoch_tombstones` with both records read off the rendered transport. That
drive is G4's deliverable and does not exist in the tree yet. An arm cannot be
run against a test that has not been written, and running it against some other
test would be grading a different object. So the recipe is recorded here, at the
point where its claims were verified against the tree, and G4 appends the
executed transcript — mutation, output, revert, green re-run — beneath it.

Everything below was re-verified against the tree at commit 4974b63d before it
was written down. Line numbers are the ones in that tree.

--------------------------------------------------------------------------------
W2 — THE EXACT MUTATION
--------------------------------------------------------------------------------

FILE: packages/server-rust/src/tombstone_frontier_impl.rs
SITE: :986, inside `drain_prunable`'s `if let Some(refs) = self.epoch_tags.remove(&e)`
      arm (the arm opens at :979).

VERIFIED AT HEAD:

    $ sed -n '986p' packages/server-rust/src/tombstone_frontier_impl.rs
                    drained_epochs.insert(e);

Unconditional on `refs.len()` — which is the antecedent's whole consequence.
The mutation gates it:

    -               drained_epochs.insert(e);
    +               if !refs.is_empty() {
    +                   drained_epochs.insert(e);
    +               }

Nothing else changes. `refs` is still owned by this arm at :986 (the borrow of
`epoch_tags` ended when `remove` returned), and `refs.len()` is read three lines
later at :990, so the gate introduces no borrow-checker cascade and no second
fold over the vector.

--------------------------------------------------------------------------------
W2 — THE EXACT DISCRIMINATING ASSERTION IT MUST RED
--------------------------------------------------------------------------------

READ OFF THE EXIT ROW, and off nothing else.

The assertion: **a `DrainedByPrune` exit whose `bytes_freed_attributed > 0`
while nothing was observed** — i.e. the drive's exit-row terms

    exit_kind              = DrainedByPrune
    bytes_freed_attributed > 0
    removed_refs_observed  = 0

WHY THE MUTATION KILLS EXACTLY THAT AND NOTHING ELSE, verified at HEAD:

  * `DrainedByPrune` is assigned ONLY where `drained_epochs.contains(&e)`:

      $ sed -n '1009,1011p' packages/server-rust/src/tombstone_frontier_impl.rs
                  let attribution = drained_epochs
                      .contains(&e)
                      .then_some(FinalExitKind::DrainedByPrune);

  * `drained_epochs` is written ONLY at :986. Under the planted antecedent
    `refs` is empty, so the gate suppresses the insert, epoch 1 reaches the
    detection sweep with `attribution == None`, and `finalize_epoch_exit`
    resolves a `None` hint to `Unclassified`.

  * Therefore the exit row's `exit_kind` flips DrainedByPrune -> Unclassified,
    and the drive's exit-row assertion fails BY NAME.

G4's failure message MUST name this assertion. A message that merely reports a
mismatched enum without stating that the discriminating property is "attributed
without observation, on the exit row" does not satisfy the arm.

--------------------------------------------------------------------------------
W2 — W2 DOES NOT MOVE `topgun_or_prune_epochs_drained_total`. NEVER GRADE IT THERE.
--------------------------------------------------------------------------------

This is the correction carried from Response v3, and it is recorded here because
grading W2 against the metrics limb would either fail the spec closed on an
authoring error or push an executor to weaken the metrics assertion until it
REDs.

`drained_epochs` feeds ONLY the exit attribution at :1009-1011. The vector the
drain RETURNS is built independently, at :996, from the removed `refs`:

    $ sed -n '996p' packages/server-rust/src/tombstone_frontier_impl.rs
                    drained.extend(refs.into_iter().map(|r| (e, r)));

and the service's per-epoch map — hence `pass.epochs_drained` — derives from
that returned vector alone:

    $ sed -n '1539p' packages/server-rust/src/service/domain/crdt.rs
            pass.considered += 1;
    $ sed -n '1704p' packages/server-rust/src/service/domain/crdt.rs
        pass.epochs_drained = per_epoch.len() as u64;

(:1539 sits inside `for (epoch, r) in drained`, and `per_epoch` is populated
only inside that same loop.)

Under the planted antecedent `refs` is EMPTY, so `drained` is empty WITH OR
WITHOUT the mutation, `per_epoch` stays empty either way, `pass.epochs_drained`
is 0 either way, and the asserted Δ = 0 on
`topgun_or_prune_epochs_drained_total` stays GREEN under W2.

CONSEQUENCE, stated as a rule: **W2 must never be graded against
`topgun_or_prune_epochs_drained_total`.** That series' own RED conditions are a
plant that did not take, a drain that returned refs, or an absent series under
an inverted recorder binding — none of which is W2. W2's discriminating
assertion is the EXIT ROW, and that is where it REDs.

--------------------------------------------------------------------------------
W2 — WHAT G4 OWES
--------------------------------------------------------------------------------

  1. Apply the :986 gate verbatim as printed above.
  2. Run the consequence drive; transcribe the REAL failure output.
  3. Confirm the message NAMES the exit-row discriminating assertion.
  4. Revert the gate; re-run; transcribe the green.
  5. Confirm — and record — that the `topgun_or_prune_epochs_drained_total`
     assertion stayed GREEN throughout, which is the positive check that this
     section's non-movement claim held in practice.

================================================================================
W2 — EXECUTED TRANSCRIPT
================================================================================

Owner group : G4 (recipe by G2)
Branch      : sf-360-origin-instrument
Tree        : 463471bc (drive at abe9dd40, limb order at 463471bc)
Status      : EXECUTED. Mutation applied, run, output transcribed, reverted,
              re-run green.

THE OBJECT UNDER TEST
  packages/server-rust/src/tombstone_frontier_impl.rs
  tests::a_planted_empty_tag_vector_diverges_attribution_from_observation_through_the_service

  The consequence drive: a present-but-empty `epoch_tags` entry planted through
  the in-module `f.lock()` bypass, then driven through the SERVICE
  (`crate::service::domain::crdt::prune_epoch_tombstones`) with a Prometheus
  recorder bound BEFORE the frontier is constructed and a `tracing` capture
  installed for the duration of the drive.

--------------------------------------------------------------------------------
STEP 1 — THE MUTATION, APPLIED VERBATIM
--------------------------------------------------------------------------------

    $ sed -n '984,989p' packages/server-rust/src/tombstone_frontier_impl.rs
                        .saturating_sub(u64::try_from(refs.len()).unwrap_or(u64::MAX));
                    self.last_drained_epoch = self.last_drained_epoch.max(e);
                    if !refs.is_empty() {
                        drained_epochs.insert(e);
                    }
                    // R1.3: `Σ tag.len()` — the same byte quantity `stamp_tombstone`

Nothing else changed. `git diff --stat` over the source file showed the gate and
nothing besides it.

--------------------------------------------------------------------------------
STEP 2 — THE RUN, AND ITS REAL OUTPUT
--------------------------------------------------------------------------------

    $ SDKROOT=$(/usr/bin/xcrun --sdk macosx --show-sdk-path) cargo test --release \
        -p topgun-server --lib \
        a_planted_empty_tag_vector_diverges_attribution_from_observation_through_the_service \
        -- --nocapture
    exit=101

    running 1 test
    CONSEQUENCE-DRIVE | exit row | target=topgun_server::tombstone_frontier::residency message=prune epoch exit kind=epoch_exit epoch=1 refs_at_entry=1 refs_at_exit=0 stamped_bytes=4 bytes_freed_attributed=0 exit_kind=Unclassified { observed_refs_delta: -1, observed_lwm: 2, observed_durable_watermark: 1, observed_current_epoch: 2, note: "epoch 1 absent from epoch_tags with no attributable removal path recorded at the detection point" } entered_at_op_seq=1 entered_at_unix_ms=1787504496087 exited_at_op_seq=2 lwm_passed_at_op_seq=Some(2) fence_passed_at_op_seq=Some(2) lwm_at_exit=2 durable_watermark_at_exit=1 current_epoch_at_exit=2 removed_refs_observed=0 removed_bytes_observed=0
    CONSEQUENCE-DRIVE | pass row | target=topgun_server::tombstone_frontier::residency message=prune pass kind=prune_pass considered=0 empty_drain=true
    CONSEQUENCE-DRIVE | pass family | topgun_or_prune_epochs_drained_total delta=0

    thread '...::a_planted_empty_tag_vector_diverges_attribution_from_observation_through_the_service'
    panicked at packages/server-rust/src/tombstone_frontier_impl.rs:5849:9:
    assertion `left == right` failed: DISCRIMINATING ASSERTION (first term) — the
    drive must observe a DrainedByPrune exit whose bytes_freed_attributed > 0 while
    removed_refs_observed = 0. Removing a present-but-empty vector is still a
    removal, so the exit must be attributed to the prune and not resolved to
    Unclassified. exit row: target=topgun_server::tombstone_frontier::residency
    message=prune epoch exit kind=epoch_exit epoch=1 refs_at_entry=1 refs_at_exit=0
    stamped_bytes=4 bytes_freed_attributed=0 exit_kind=Unclassified { ... } ...
    removed_refs_observed=0 removed_bytes_observed=0
      left: "Unclassified"
     right: "DrainedByPrune"

    test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured;
    1860 filtered out; finished in 0.20s

--------------------------------------------------------------------------------
STEP 3 — THE MESSAGE NAMES THE DISCRIMINATING ASSERTION
--------------------------------------------------------------------------------

CONFIRMED, and off the EXIT ROW specifically.

The panic text is literally

    DISCRIMINATING ASSERTION (first term) — the drive must observe a
    DrainedByPrune exit whose bytes_freed_attributed > 0 while
    removed_refs_observed = 0

so the arm does not merely fail: it says WHICH claim it is failing, states the
claim in the three-term form G2's recipe pinned, and prints the whole rendered
exit row it read the term off. The rendered row is its own corroboration —

    bytes_freed_attributed=0
    exit_kind=Unclassified { observed_refs_delta: -1, ... note: "epoch 1 absent
              from epoch_tags with no attributable removal path recorded at the
              detection point" }

which is exactly the mechanism G2's recipe predicted from source: the gate
suppresses `drained_epochs.insert(e)` for an empty `refs`, epoch 1 reaches the
detection sweep with `attribution == None`, and `finalize_epoch_exit` resolves a
`None` hint to `Unclassified` — carrying, in the variant's own `note`, the raw
observed state rather than a silent default.

Two of the three terms move together, and the transcript records both: the
attribution collapses from `DrainedByPrune` to `Unclassified` AND
`bytes_freed_attributed` collapses from 4 to 0. The first term is what the
assertion trips on because it is asserted first; the second is visible on the
same printed row.

--------------------------------------------------------------------------------
STEP 4 — X21-c STAYED GREEN UNDER THE MUTATION
--------------------------------------------------------------------------------

This is the positive check G2's recipe item 5 owed, and it is witnessed in the
SAME mutated run rather than argued.

The drive grades the pass-counter family BEFORE the exit row, so the mutated run
had to get past all four series' deltas to reach the panic. It did, and it
printed what it read:

    CONSEQUENCE-DRIVE | pass family | topgun_or_prune_epochs_drained_total delta=0

Asserted in that helper, all green under the mutation:

    topgun_or_prune_epochs_drained_total    delta = 0
    topgun_or_prune_considered_total        delta = 0
    topgun_or_prune_empty_drains_total      delta = 1
    topgun_or_prune_nonempty_drains_total   delta = 0

each read as `Some("…")` off both the pre-drive and the post-drive render — an
absent series is a hard error in this reader, never a zero.

So the non-movement claim held IN PRACTICE, not only on paper: W2 gates
`drained_epochs.insert(e)`, `drained_epochs` feeds only the exit attribution,
and `pass.epochs_drained` derives from the independently-built returned vector,
which is empty with or without the gate. W2 is NOT a RED trigger for X21-c, and
the run proves it by passing that limb and failing the next one.

The `empty_drains` delta of 1 is the run's own non-vacuity evidence: the pass
really executed and really read its drain as empty, so the three zeroes beside
it are observations rather than the silence of a pass that never happened.

--------------------------------------------------------------------------------
STEP 5 — REVERT, AND THE GREEN RE-RUN
--------------------------------------------------------------------------------

    $ sed -n '984,987p' packages/server-rust/src/tombstone_frontier_impl.rs
                        .saturating_sub(u64::try_from(refs.len()).unwrap_or(u64::MAX));
                    self.last_drained_epoch = self.last_drained_epoch.max(e);
                    drained_epochs.insert(e);
                    // R1.3: `Σ tag.len()` — the same byte quantity `stamp_tombstone`

    $ SDKROOT=$(/usr/bin/xcrun --sdk macosx --show-sdk-path) cargo test --release \
        -p topgun-server --lib \
        a_planted_empty_tag_vector_diverges_attribution_from_observation_through_the_service \
        -- --nocapture
    exit=0

    running 1 test
    CONSEQUENCE-DRIVE | exit row | target=topgun_server::tombstone_frontier::residency message=prune epoch exit kind=epoch_exit epoch=1 refs_at_entry=1 refs_at_exit=0 stamped_bytes=4 bytes_freed_attributed=4 exit_kind=DrainedByPrune entered_at_op_seq=1 entered_at_unix_ms=1787504793906 exited_at_op_seq=2 lwm_passed_at_op_seq=Some(2) fence_passed_at_op_seq=Some(2) lwm_at_exit=2 durable_watermark_at_exit=1 current_epoch_at_exit=2 removed_refs_observed=0 removed_bytes_observed=0
    CONSEQUENCE-DRIVE | pass row | target=topgun_server::tombstone_frontier::residency message=prune pass kind=prune_pass considered=0 empty_drain=true
    CONSEQUENCE-DRIVE | pass family | topgun_or_prune_epochs_drained_total delta=0
    CONSEQUENCE-DRIVE | class | AttributedWithoutObservation
    test ... ok

    test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured;
    1860 filtered out; finished in 0.20s

And the whole suite, unmutated:

    $ SDKROOT=$(/usr/bin/xcrun --sdk macosx --show-sdk-path) cargo test --release \
        -p topgun-server --lib tombstone_frontier
    test result: ok. 73 passed; 0 failed; 0 ignored; 0 measured;
    1788 filtered out; finished in 11.19s

--------------------------------------------------------------------------------
W2 — VERDICT
--------------------------------------------------------------------------------

EARNED. The consequence drive REDs when `:986`'s unconditional insert is gated
on `!refs.is_empty()`, its failure message NAMES the exit-row discriminating
assertion in the three-term form, and the same run shows the
`topgun_or_prune_epochs_drained_total` limb passing with delta 0 — so the arm
discriminates exactly the claim it was written to discriminate and nothing
adjacent to it. Reverted; green.

WHAT THIS ARM DOES NOT CLAIM. It is a mutation arm on a CONTROL, not a
discovery. Reachability of the `Some(vec![])` state was settled elsewhere; what
the drive and this arm establish is that WHEN the state is reached, the service
path really does produce the divergent pair, and that the drive would notice if
it stopped doing so.

--------------------------------------------------------------------------------
G4 — THE CONSEQUENCE DRIVE'S OWN RECORD (inputs G5 and G6 need)
--------------------------------------------------------------------------------

CAPTURE / EXTRACTION HELPER CHOICE — `row_field`-SHAPED, ADDED IN-FILE.

Of the two options the Delta ledgered, G4 took the SECOND: field-extraction
helpers of the `row_field` shape, added inside `tombstone_frontier_impl.rs`'s
own test module, rather than the `captured_tracing_events` +
`contains("name=value")` idiom. Four helpers, all `#[cfg(test)]`-module-private,
consuming no additional counted `.rs`:

    fn row_field<'a>(row: &'a str, name: &str) -> &'a str
    fn row_u64(row: &str, name: &str) -> u64
    fn row_bool(row: &str, name: &str) -> bool
    fn rows_of_kind<'a>(rows: &'a [String], target: &str, kind: &str) -> Vec<&'a str>
    fn rendered_counter_u64(rendered: &str, name: &str, when: &str) -> u64

WHY, in one line: a `contains("exit_kind=DrainedByPrune")` miss cannot say WHICH
value it found, and the mutation arm above is graded on its message naming the
discriminating assertion — a substring reader would have produced "assertion
failed: row.contains(...)" and nothing more.

The two cross-module variants (`sim/tombstone_gc_proof.rs:827-873` and the one
in `service/domain/crdt.rs`) are `#[cfg(test)]`-module-private and are NOT
importable here; none was moved, re-exported or otherwise made reachable.

THE RENDERED FORM THESE HELPERS CONSUME — three properties, all verified
against the real capture above:

  1. TARGET PREFIX, NO LEADING SPACE. This file's `FieldTextVisitor` seeds its
     buffer with `format!("target={} ", target)`, so `target=…` is the row's
     FIRST token and is shaped like any other pair. (The sim file's visitor
     prefixes `" target={} "` WITH a leading space; the helpers here consume
     this file's form.)

  2. PAIRS ARE WHITESPACE-SEPARATED AND THE PREFIX IS ANCHORED AT A TOKEN
     BOUNDARY. `row_field` splits on whitespace and strips `"{name}="` from each
     token, so `kind` cannot match `exit_kind` and `epoch` cannot match
     `epochs_drained` — both near-misses are live on the exit row.

  3. THE `message` FIELD CONTAINS SPACES, AND THAT IS SAFE HERE BUT IS A HAZARD
     TO KNOW. Every captured row begins
     `target=… message=prune epoch exit kind=epoch_exit …`, so the message's
     words (`prune`, `epoch`, `exit`) appear as BARE tokens with no `=`. They
     cannot be mistaken for a pair — but a future field whose name collides with
     a word in some message, or a message word carrying an `=`, would defeat
     this reader. G5's new removal line should keep its message free of `=`.

     `exit_kind`'s `Unclassified { … }` rendering ALSO contains spaces (see the
     mutated run above). `row_field(row, "exit_kind")` returns just
     `Unclassified` in that case, which is exactly enough for the assertion to
     fail informatively, and every pair after it on the row is still intact.

WHAT G4 CHANGED IN THE SHARED FIXTURE. `drain_one_epoch_fixture` was SPLIT: the
prelude and the `f.lock()` plant now live in `one_epoch_frontier(plant) ->
TombstoneFrontier`, and `drain_one_epoch_fixture` calls it and then drains. Two
reasons a fused helper cannot serve — the frontier must OUTLIVE the plant for a
service-level drive, and it must be CONSTRUCTED inside the caller's own recorder
binding, because metric handles resolve once at construction. W3's expected
values are unchanged and W3 is green on the split fixture.

THE VERBATIM ROWS G5 AND G6 NEED (unmutated tree, green run above):

  exit row:
    target=topgun_server::tombstone_frontier::residency message=prune epoch exit kind=epoch_exit epoch=1 refs_at_entry=1 refs_at_exit=0 stamped_bytes=4 bytes_freed_attributed=4 exit_kind=DrainedByPrune entered_at_op_seq=1 entered_at_unix_ms=1787504793906 exited_at_op_seq=2 lwm_passed_at_op_seq=Some(2) fence_passed_at_op_seq=Some(2) lwm_at_exit=2 durable_watermark_at_exit=1 current_epoch_at_exit=2 removed_refs_observed=0 removed_bytes_observed=0

  pass row:
    target=topgun_server::tombstone_frontier::residency message=prune pass kind=prune_pass considered=0 empty_drain=true

  (both rows end with a single trailing space — the visitor writes `name=value `
  per pair and never trims.)

  class printed: AttributedWithoutObservation

CAPTURE CENSUS. The drive's capture wraps `prune_epoch_tombstones` and NOTHING
else — the fixture's own stamps fire their entry rows before the capture is
installed. Under the planted antecedent the capture holds exactly TWO rows: one
`kind=epoch_exit` and one `kind=prune_pass`, and zero settlement rows (the
settlement row is emitted per DRAINED epoch, and the drain returned none). The
non-vacuity gate asserts non-empty plus that per-kind census before any term is
read.

================================================================================
THE ORIGIN SIGNATURE — the planted antecedent's own removal line
================================================================================

Owner group : G5
Branch      : sf-360-origin-instrument
Test        : tombstone_frontier_impl::tests::
              the_removal_line_carries_the_origin_signature_for_a_present_but_empty_entry

Not a mutation arm. This is the reading the instrument exists to make visible,
recorded here because the three readings only mean something as a SET.

The fixture is the one G4's drive already uses: epoch 1 rolls over holding one
ref (so its slot records `refs_at_entry = 1`), and its `epoch_tags` entry is then
replaced, through the in-module lock bypass, with a PRESENT-BUT-EMPTY vector. The
drive goes through the production prune service, with the capture wrapping the
service entry point and nothing else.

COMMAND
  SDKROOT=$(/usr/bin/xcrun --sdk macosx --show-sdk-path) \
    cargo test --release -p topgun-server --lib removal_line -- --nocapture

RENDERED LINE (verbatim; `ts` varies per run, every other term is fixed by the
fixture; note the single TRAILING space — the visitor writes `name=value ` per
pair and never trims):

target=topgun_server::tombstone_frontier::removal message=prune removal observed ts=1787505799565 op_seq=2 epoch=1 refs_returned=0 refs_at_entry=1 bytes_returned=0 watermark=1 ceiling=2 

READING: `refs_returned = 0` beside `refs_at_entry = 1`. The removal returned an
EMPTY vector for an epoch that entered the index holding refs. That pair on ONE
line is the origin signature the frozen extractor contract pins, and it is now
visible without a reader having to join two rows.

The line is selected by TARGET ALONE. It carries no `kind` discriminant, because
its own target does that work — which is what keeps the field set at exactly the
eight it was adopted with rather than widening it for the reader's convenience.

Result: PASS.


================================================================================
W8 — THE INSTRUMENT'S POSITIVE CONTROL
================================================================================

Owner group : G5
Test        : tombstone_frontier_impl::tests::
              the_removal_line_reports_a_genuinely_non_empty_removal_as_positive

The same one-epoch fixture, UNPLANTED, driven through the same production prune
service. Epoch 1's index entry holds its one genuine ref, so the removal returns
a non-empty vector.

RENDERED LINE (verbatim):

target=topgun_server::tombstone_frontier::removal message=prune removal observed ts=1787505799565 op_seq=2 epoch=1 refs_returned=1 refs_at_entry=1 bytes_returned=4 watermark=1 ceiling=2 

ASSERTED, on the rendered text, BY FIELD NAME: `refs_returned > 0` AND
`refs_at_entry > 0` AND `bytes_returned > 0`.

WHY THIS ARM EXISTS. Without it, a downstream reader of a window containing no
zero-returned lines could not tell "the arm was reached and always returned refs"
from "this instrument cannot report a positive at all". With it, and with the
negative control below, all THREE readings the instrument can produce — silent,
zero-returned, positive — are exercised before it ships.

Result: PASS.


================================================================================
W4 — THE INSTRUMENT'S NEGATIVE CONTROL, ON BOTH PATHS THAT SKIP THE ARM
================================================================================

Owner group : G5
Test        : tombstone_frontier_impl::tests::
              the_removal_line_is_absent_on_both_paths_that_skip_the_removal_arm

This is the arm that makes SILENCE from the instrument meaningful rather than
ambiguous. Both paths that reach the drain without reaching the removal site are
covered, and they are covered separately because they are different mechanisms.

ARM (a) — NO ELIGIBLE EPOCH, with the watermark NOT zero.
  The unplanted fixture is driven twice through the prune service. The FIRST pass
  removes epoch 1 — and the test ASSERTS that it emitted exactly one removal
  line, so the arm's premise (there is nothing left to remove) is ESTABLISHED
  rather than assumed. The SECOND pass finds an empty eligible set while the
  durable watermark is still 1, so the silence is attributable to ELIGIBILITY and
  not to the dark fast path.
  Observed: ZERO rows on the removal target; the capture itself NON-EMPTY.

ARM (b) — THE DARK FAST PATH.
  A frontier whose durable watermark is left at 0. The drain returns before it
  opens a sweep at all, so no epoch is even considered.
  Observed: ZERO rows on the removal target; the capture itself NON-EMPTY.

THE NON-VACUITY GATE, on both arms. Each arm asserts its capture is NON-EMPTY
BEFORE reading an absence off it. A drive whose transport produced nothing would
therefore RED rather than pass as "no removal lines" — an absence read off an
empty capture is not evidence of anything.

Result: PASS, both arms.


================================================================================
W5 — THE INSTRUMENT'S OBSERVATION ARM  (the most important witness here)
================================================================================

Owner group : G5

WHAT IT PROVES. That `refs_returned` is an OBSERVATION — read from the vector the
index removal itself returned — and not a second copy of the entry-side slot. If
both terms came from the slot, the line would reproduce, in a new place, exactly
the tautology this family has been trying to escape: a self-consistent and
perfectly misleading pair of numbers that can never disagree.

MUTATION APPLIED, verbatim (in `drain_prunable`'s `Some(refs)` arm):

    -                    refs_returned = removed_refs,
    +                    refs_returned = self.epoch_slots.get(&e).map_or(0, |s| s.refs_at_entry),
                         refs_at_entry = self.epoch_slots.get(&e).map_or(0, |s| s.refs_at_entry),

This is the MINIMAL faithful encoding of the tautology: one expression swapped
for the one beside it, nothing else touched.

OUTPUT, verbatim:

  ORIGIN-INSTRUMENT | planted removal row | target=topgun_server::tombstone_frontier::removal message=prune removal observed ts=1787506117445 op_seq=2 epoch=1 refs_returned=1 refs_at_entry=1 bytes_returned=0 watermark=1 ceiling=2 
  ORIGIN-INSTRUMENT | positive removal row | target=topgun_server::tombstone_frontier::removal message=prune removal observed ts=1787506117445 op_seq=2 epoch=1 refs_returned=1 refs_at_entry=1 bytes_returned=4 watermark=1 ceiling=2 

  thread 'tombstone_frontier_impl::tests::the_removal_line_carries_the_origin_signature_for_a_present_but_empty_entry' (3291926) panicked at packages/server-rust/src/tombstone_frontier_impl.rs:6192:9:
  assertion `left == right` failed: OBSERVATION ARM — `refs_returned != refs.len()`: the line must report what the index removal actually returned (an EMPTY vector here), not the entry-side `refs_at_entry` the slot still carries. A line on which refs_returned mirrors refs_at_entry is a second copy of the slot, not an observation: target=topgun_server::tombstone_frontier::removal message=prune removal observed ts=1787506117445 op_seq=2 epoch=1 refs_returned=1 refs_at_entry=1 bytes_returned=0 watermark=1 ceiling=2 
    left: 1
   right: 0

  test tombstone_frontier_impl::tests::the_removal_line_is_absent_on_both_paths_that_skip_the_removal_arm ... ok
  test tombstone_frontier_impl::tests::the_removal_line_reports_a_genuinely_non_empty_removal_as_positive ... ok
  test tombstone_frontier_impl::tests::the_removal_line_carries_the_origin_signature_for_a_present_but_empty_entry ... FAILED

  test result: FAILED. 2 passed; 1 failed; 0 ignored; 0 measured; 1861 filtered out

THE DISCRIMINATING ASSERTION IS NAMED IN THE FAILURE TEXT, verbatim:
`refs_returned != refs.len()`.

WHICH ARM DISCRIMINATES, recorded because it is informative rather than
incidental: the POSITIVE control stayed GREEN under this mutation. In that arm
`refs.len()` and `refs_at_entry` are both 1, so the swap is invisible there. Only
the ORIGIN-SIGNATURE arm — where the observation is 0 and the slot says 1 —
discriminates. A witness set that carried only a positive control would have
graded this mutation green and shipped a tautology believing it was an
observation.

REVERT
  git checkout packages/server-rust/src/tombstone_frontier_impl.rs

RE-RUN AFTER REVERT (verbatim):

  ORIGIN-INSTRUMENT | positive removal row | target=topgun_server::tombstone_frontier::removal message=prune removal observed ts=1787505799565 op_seq=2 epoch=1 refs_returned=1 refs_at_entry=1 bytes_returned=4 watermark=1 ceiling=2 
  ORIGIN-INSTRUMENT | planted removal row | target=topgun_server::tombstone_frontier::removal message=prune removal observed ts=1787505799565 op_seq=2 epoch=1 refs_returned=0 refs_at_entry=1 bytes_returned=0 watermark=1 ceiling=2 
  test tombstone_frontier_impl::tests::the_removal_line_reports_a_genuinely_non_empty_removal_as_positive ... ok
  test tombstone_frontier_impl::tests::the_removal_line_is_absent_on_both_paths_that_skip_the_removal_arm ... ok
  test tombstone_frontier_impl::tests::the_removal_line_carries_the_origin_signature_for_a_present_but_empty_entry ... ok

  test result: ok. 3 passed; 0 failed; 0 ignored; 0 measured; 1861 filtered out

Result: RED under the mutation, naming its discriminating assertion; GREEN after
revert. EARNED.


================================================================================
W6 — ARM SELECTION.  IMPLEMENTATION IS G6's; THIS SECTION RECORDS THE DECISION
      AND THE EMPIRICAL EVIDENCE BEHIND IT.
================================================================================

Owner group : G5 (selection only)

SELECTED: **PRIMARY** — the spawned drain future carries the capture subscriber
with it, and the sim leg asserts the branch's RENDERED LINE.

The selection was made EMPIRICALLY, not by reading. A throwaway probe was
inserted into the sim file, compiled, run in BOTH directions, and then removed;
the sim file is byte-unedited at handoff (its blob hash is unchanged from the
base commit, verified with `git rev-parse HEAD:<path>`).

PRECONDITIONS VERIFIED FIRST
  - `tracing` resolves to 0.1.44 in Cargo.lock, so
    `tracing::instrument::WithSubscriber` is available.        CONFIRMED
  - Uses of `with_subscriber` / `WithSubscriber` anywhere under
    packages/server-rust/src: ZERO. This is a new-to-the-tree
    construct and is called out as one.                        CONFIRMED

THE PROBE. The existing race scaffold was used unchanged in shape: a shared
2-party barrier, a spawned cursor ACK and a spawned prune pass released together,
under the existing `#[tokio::test(flavor = "multi_thread")]` flavour. The only
change was the one the PRIMARY arm calls for:

    let subscriber = tracing_subscriber::registry().with(RowCapture(sink.clone()));
    let dispatch   = tracing::Dispatch::new(subscriber);
    let _guard     = tracing::dispatcher::set_default(&dispatch);   // the test thread
    ...
    let sweep = tokio::spawn(
        async move { barrier.wait(); prune_epoch_tombstones(..).await; }
            .with_subscriber(dispatch.clone()),                     // the spawned task
    );

  — i.e. the `Dispatch` is built ONCE, a reference goes to the test thread via
  `tracing::dispatcher::set_default`, and a CLONE travels with the spawned
  future. The runtime flavour and the scaffold's thread model are UNCHANGED.

COMMAND
  SDKROOT=$(/usr/bin/xcrun --sdk macosx --show-sdk-path) \
    cargo test --profile ci-sim --features simulation -p topgun-server --lib \
    -- probe_primary_arm --nocapture

DIRECTION 1 — WITH `.with_subscriber(dispatch)` (output verbatim):

  PROBE | total rows 16 | removal rows 5
  PROBE |  target=topgun_server::tombstone_frontier::removal message=prune removal observed ts=1787506278401 op_seq=10 epoch=1 refs_returned=1 refs_at_entry=1 bytes_returned=2 watermark=1000 ceiling=6 
  PROBE |  target=topgun_server::tombstone_frontier::removal message=prune removal observed ts=1787506278401 op_seq=10 epoch=3 refs_returned=1 refs_at_entry=1 bytes_returned=2 watermark=1000 ceiling=6 
  PROBE |  target=topgun_server::tombstone_frontier::removal message=prune removal observed ts=1787506278401 op_seq=10 epoch=2 refs_returned=1 refs_at_entry=1 bytes_returned=2 watermark=1000 ceiling=6 
  PROBE |  target=topgun_server::tombstone_frontier::removal message=prune removal observed ts=1787506278401 op_seq=10 epoch=4 refs_returned=1 refs_at_entry=1 bytes_returned=2 watermark=1000 ceiling=6 
  PROBE |  target=topgun_server::tombstone_frontier::removal message=prune removal observed ts=1787506278401 op_seq=10 epoch=5 refs_returned=1 refs_at_entry=1 bytes_returned=2 watermark=1000 ceiling=6 
  test sim::tombstone_gc_proof::tests::probe_primary_arm_with_subscriber ... ok

DIRECTION 2 — THE SAME PROBE WITHOUT `.with_subscriber(...)`, everything else
identical (output verbatim):

  PROBE | total rows 0 | removal rows 0

  (the assertion was inverted for this direction so the run would report rather
  than abort; the number that matters is the ZERO.)

WHAT THE TWO DIRECTIONS ESTABLISH TOGETHER. The rendered line is observable
across the spawn IF AND ONLY IF the subscriber travels with the future. Direction
2 reproduces exactly the empty capture the sim file's own doc-contract predicts
for a thread-local `set_default` under a multi-threaded runtime — so a sim leg
that asserted a rendered line over a spawned drain WITHOUT carrying the
subscriber would be reading an empty capture and passing vacuously. Direction 1
shows the mechanism closes that gap. The selection is therefore evidenced in both
directions, not merely in the one that succeeds.

Note the rendered form in the probe output: rows captured by the SIM file's
visitor begin with a LEADING SPACE before `target=`, unlike the frontier file's
visitor. G6's sim-side reader must consume the leading-space form.

WHY PRIMARY RATHER THAN THE FALLBACK. The instruction is explicit that there is
no silent third option and that if PRIMARY is selectable it is selected. It is
selectable: the adaptation is confined to the already-counted sim file, needs no
production edit, no new counted file, no change of runtime flavour and no change
to the scaffold's thread model — and it grades the sim leg at LINE granularity,
which is strictly stronger than the fallback's metrics granularity for an
instrument whose whole deliverable is a rendered line.

WHAT G6 STILL OWES ON THIS ARM (the selection does not discharge the witness):
  - the leg proper: read which of the two barrier orderings the round took and
    assert THAT branch's consequences, never a disjunction both branches satisfy;
  - the non-emptiness gate BEFORE any term is read, so an unobserved transport
    REDs instead of passing;
  - the mutation arm: with the observation mutation applied, this leg must RED
    too;
  - the module doc-contract re-wording the arm entails (recorded in the ruling
    artifact's post-section).

Result: PRIMARY SELECTED, empirically, both directions. Probe removed; sim file
byte-unedited.

================================================================================
(next witness section appends below this line)
================================================================================
